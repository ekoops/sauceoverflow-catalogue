mutation {
  createProduct(createProductInput: {name:"ciao", description:"ciao", price: 1.0, category:STYLE}) {
    _id
  name
  createdAt
  description
  price
  comments {
     _id,
      title,
      body,
      stars,
      date
  }
  category
  stars
  }
}

mutation {
  createComment(productId:"60ac1af84c035e31b064e411", createCommentInput:{title: "ciao", stars: 1}) {
      _id,
      title,
      body,
      stars,
      date
  }
}